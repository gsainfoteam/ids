---
'@gsainfoteam/ids-react': minor
---

Add TextArea as a container with a `TextArea.Input` sentinel, matching TextField. Children before and after the input become top and bottom bars; the textarea grows with its content by default (`autoResize`, `rows`, `maxRows`) or uses manual `resize`.
