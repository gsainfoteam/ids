---
"@gsainfoteam/ids-react": minor
---

Add Field anatomy with automatic label/description/error associations, state propagation,
size inheritance and an optional react-hook-form entry point for native and controlled inputs.
Preserve React 19 callback-ref cleanup when composing refs.

Include Zod resolver examples and regression coverage for nested and controlled fields, cross-field validation, parsed submission values, and reset.
