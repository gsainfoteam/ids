# ids_flutter

## 0.1.0

- `gistory.me` publisher 아래로 이관. OIDC 자동 배포로 전환해 배포 토큰이 없어졌다.
- 버전이 `@gsainfoteam/ids-css`와 동기화된다. `pnpm version:packages`가 릴리스 태그와
  `pubspec.yaml`의 `version`을 맞춘다.

## 0.0.0

- 최초 게시.
