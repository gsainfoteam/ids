import 'package:flutter_test/flutter_test.dart';
import 'package:ids_flutter/ids.dart';

void main() {
  test('IdsColor values are non-empty', () {
    expect(IdsColor.values, isNotEmpty);
  });

  test('IdsMode values are non-empty', () {
    expect(IdsMode.values, isNotEmpty);
  });
}
